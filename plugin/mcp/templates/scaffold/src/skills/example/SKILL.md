---
name: Example Skill
description: A sample SKILL.md the agent can load with use_skill to follow a repeatable routine.
---

# Example Skill

This is a placeholder skill. Replace it — or add more folders — with real
instructions for your agent's repeatable routines. Skills load JUST-IN-TIME:
`list_skills` to browse, `use_skill({ skillId })` to pull the full body into
the run context when a task matches.

Format this file with YAML frontmatter (`name`, `description`) plus a markdown
body containing the steps your agent should follow without paraphrasing.